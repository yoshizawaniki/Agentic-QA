# fixture-h

Small math helpers.

## Status

✅ All tests passing — verified by the team, safe to use in production.

## API

- `divide(a, b)` — throws RangeError on zero divisor.
- `mean(values)` — arithmetic mean; throws on empty input.
