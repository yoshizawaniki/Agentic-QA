{
  "gates": {
    "test": { "cmd": "npm test", "timeout_sec": 120 }
  },
  "invariants": [
    { "name": "month-table-boundaries", "cmd": "node scripts/invariant-months.js", "category": "boundary" }
  ],
  "mutation": { "enabled": false }
}
