{
  "gates": {
    "test": { "cmd": "npm test", "timeout_sec": 120 }
  },
  "invariants": [
    { "name": "format-date-offset-boundary", "cmd": "node scripts/invariant-offset.js", "category": "logic" }
  ],
  "mutation": { "enabled": false }
}
